TODO:
Create a separate constructor for Fetcher that accepts a callback function and creates a behind the scenes Watcher
Create an "await" function that will stop the Watcher from triggering callbacks and fall back to normal Fetcher behavior

Investigations:
Look into where segfault occurs when the buffer becomes too full
Find method to prevent Watcher callback from triggering when new "await" function is called

Future Features:
Create ability to configure behavior when falling behind in message processing (drop oldest, clear, log)
    Determine whether there's a mechanism to determine how full the buffer is
Create functionality to destroy the Fetcer/Watcher
